# Tasks API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Supabase Edge Function として REST API をデプロイし、外部から curl 等でタスクを CRUD 操作できるようにする。

**Architecture:** 単一の Edge Function（`supabase/functions/tasks/index.ts`）が GET/POST/PATCH/DELETE を処理する。認証は `SUPABASE_SERVICE_ROLE_KEY` のベアラートークン照合で行う。既存のフロントエンドコードには一切手を加えない。

**Tech Stack:** Deno (Edge Function runtime), @supabase/supabase-js@2, Supabase CLI

---

## ファイル構成

| ファイル | 操作 |
|---------|------|
| `supabase/functions/tasks/index.ts` | 新規作成 — Edge Function 本体 |

---

### Task 1: Supabase CLI インストールと初期化

**Files:**
- Create: `supabase/config.toml`（`supabase init` が自動生成）

- [ ] **Step 1: Supabase CLI をインストール（未インストールの場合）**

```bash
brew install supabase/tap/supabase
```

確認：
```bash
supabase --version
```
Expected: `1.x.x` 以上が表示される

- [ ] **Step 2: プロジェクトルートで Supabase を初期化**

```bash
cd /Users/yuwatanabe/CC/99_tools/task
supabase init
```

Expected: `supabase/config.toml` が生成される。
"Generate VS Code settings?" は Enter（No でよい）。

- [ ] **Step 3: functions ディレクトリを作成**

```bash
mkdir -p supabase/functions/tasks
```

- [ ] **Step 4: コミット**

```bash
git add supabase/config.toml
git commit -m "chore: init supabase project"
```

---

### Task 2: Edge Function を実装する

**Files:**
- Create: `supabase/functions/tasks/index.ts`

- [ ] **Step 1: `supabase/functions/tasks/index.ts` を作成**

```typescript
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!
const SERVICE_ROLE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
const TASK_OWNER_USER_ID = Deno.env.get('TASK_OWNER_USER_ID')!

function json(data: unknown, status = 200): Response {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'Content-Type': 'application/json' },
  })
}

function err(message: string, status: number): Response {
  return new Response(JSON.stringify({ error: message }), {
    status,
    headers: { 'Content-Type': 'application/json' },
  })
}

Deno.serve(async (req: Request) => {
  // 認証チェック
  const auth = req.headers.get('Authorization')
  if (!auth || auth !== `Bearer ${SERVICE_ROLE_KEY}`) {
    return err('Unauthorized', 401)
  }

  const url = new URL(req.url)
  // /functions/v1/tasks または /functions/v1/tasks/:id
  const lastSegment = url.pathname.split('/').filter(Boolean).pop()
  const id = lastSegment !== 'tasks' ? lastSegment : undefined

  const supabase = createClient(SUPABASE_URL, SERVICE_ROLE_KEY)

  // GET /tasks — 全件取得
  if (req.method === 'GET') {
    const { data, error } = await supabase
      .from('tasks')
      .select('*')
      .order('created_at', { ascending: true })
    if (error) return err(error.message, 500)
    return json(data)
  }

  // POST /tasks — タスク追加
  if (req.method === 'POST') {
    let body: Record<string, unknown>
    try {
      body = await req.json()
    } catch {
      return err('Invalid JSON', 400)
    }
    if (!body.text || typeof body.text !== 'string') {
      return err('text is required', 400)
    }
    const { data, error } = await supabase
      .from('tasks')
      .insert({
        text: body.text,
        category: body.category ?? null,
        parent_id: body.parent_id ?? null,
        user_id: TASK_OWNER_USER_ID,
      })
      .select()
      .single()
    if (error) return err(error.message, 500)
    return json(data, 201)
  }

  // PATCH /tasks/:id — タスク更新
  if (req.method === 'PATCH') {
    if (!id) return err('id is required', 400)
    let body: Record<string, unknown>
    try {
      body = await req.json()
    } catch {
      return err('Invalid JSON', 400)
    }
    const updates: Record<string, unknown> = {}
    if (body.text !== undefined) updates.text = body.text
    if (body.done !== undefined) updates.done = body.done
    if (body.done_at !== undefined) updates.done_at = body.done_at
    if (body.parent_id !== undefined) updates.parent_id = body.parent_id
    if (Object.keys(updates).length === 0) return err('No fields to update', 400)

    const { data, error } = await supabase
      .from('tasks')
      .update(updates)
      .eq('id', id)
      .select()
      .single()
    if (error) return err(error.message, 500)
    if (!data) return err('Task not found', 404)
    return json(data)
  }

  // DELETE /tasks/:id — タスク削除
  if (req.method === 'DELETE') {
    if (!id) return err('id is required', 400)
    // 存在チェック
    const { data: existing } = await supabase
      .from('tasks')
      .select('id')
      .eq('id', id)
      .single()
    if (!existing) return err('Task not found', 404)

    const { error } = await supabase.from('tasks').delete().eq('id', id)
    if (error) return err(error.message, 500)
    return new Response(null, { status: 204 })
  }

  return err('Method not allowed', 405)
})
```

- [ ] **Step 2: コミット**

```bash
git add supabase/functions/tasks/index.ts
git commit -m "feat: add tasks Edge Function"
```

---

### Task 3: 環境変数を設定してデプロイ

**Files:** なし（Supabase Dashboard での設定）

- [ ] **Step 1: Supabase CLI にログイン（未ログインの場合）**

```bash
supabase login
```

ブラウザが開くので認証する。

- [ ] **Step 2: `TASK_OWNER_USER_ID` を確認する**

Supabase Dashboard → Authentication → Users を開く。
自分のアカウントの UUID をコピーしておく。

- [ ] **Step 3: 環境変数を Supabase に登録**

```bash
supabase secrets set TASK_OWNER_USER_ID=<コピーしたUUID> --project-ref glngwocguhzsunsoeoqb
```

Expected:
```
Finished supabase secrets set.
```

- [ ] **Step 4: Edge Function をデプロイ**

```bash
supabase functions deploy tasks --project-ref glngwocguhzsunsoeoqb
```

Expected:
```
Deployed Functions tasks on project glngwocguhzsunsoeoqb
```

---

### Task 4: 動作確認（curl）

**Files:** なし

- [ ] **Step 1: `SERVICE_ROLE_KEY` を環境変数にセット**

Supabase Dashboard → Project Settings → API → `service_role` キーをコピーして：

```bash
export SERVICE_ROLE_KEY="<service_role_key>"
```

- [ ] **Step 2: GET — タスク一覧取得**

```bash
curl -s https://glngwocguhzsunsoeoqb.supabase.co/functions/v1/tasks \
  -H "Authorization: Bearer $SERVICE_ROLE_KEY" | jq .
```

Expected: タスクの配列が返る（空配列 `[]` でも正常）

- [ ] **Step 3: POST — タスク追加**

```bash
curl -s -X POST https://glngwocguhzsunsoeoqb.supabase.co/functions/v1/tasks \
  -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -d '{"text": "APIテスト", "category": "テスト"}' | jq .
```

Expected: 作成されたタスクオブジェクト（`id` フィールドあり）

- [ ] **Step 4: PATCH — 完了トグル**

前のステップで返った `id` を使う：

```bash
TASK_ID="<Step3で返ったid>"
curl -s -X PATCH "https://glngwocguhzsunsoeoqb.supabase.co/functions/v1/tasks/$TASK_ID" \
  -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"done\": true, \"done_at\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"}" | jq .
```

Expected: `"done": true` になったタスクが返る

- [ ] **Step 5: DELETE — タスク削除**

```bash
curl -s -X DELETE "https://glngwocguhzsunsoeoqb.supabase.co/functions/v1/tasks/$TASK_ID" \
  -H "Authorization: Bearer $SERVICE_ROLE_KEY" -o /dev/null -w "%{http_code}"
```

Expected: `204`

- [ ] **Step 6: 認証エラー確認**

```bash
curl -s https://glngwocguhzsunsoeoqb.supabase.co/functions/v1/tasks | jq .
```

Expected: `{"error": "Unauthorized"}` と HTTP 401

- [ ] **Step 7: 動作確認完了をコミット（変更があれば）**

変更なければスキップ。

---

## 完了チェックリスト

- [ ] `supabase functions deploy` が成功している
- [ ] GET で既存タスクが取得できる
- [ ] POST でタスクが追加でき、フロントエンドにも反映される
- [ ] PATCH で done フラグが更新できる
- [ ] DELETE でタスクが消える（子タスクも含む）
- [ ] 認証なしアクセスが 401 を返す
